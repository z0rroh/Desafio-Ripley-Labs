import React from 'react';

const ProductDetailComponent = ({ product }) => {
   return (
      <div>
         detalle producto! { product.name }
      </div>
   );
};

export default ProductDetailComponent;